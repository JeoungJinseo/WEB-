# OVEN SAUNA 이미지 교체본

첫 번째 데모(`/`)에 제공된 그래픽 6개를 다음 순서로 적용했습니다.

1. 스티커
2. 티켓
3. 표지판
4. 눈
5. 김 서린 유리
6. 수건

## 실행

Node.js 22.12 이상에서 프로젝트 폴더를 열고 실행합니다.

```sh
npm install
npm run dev
```

브라우저에서 터미널에 표시된 로컬 주소를 열고 아래로 스크롤합니다.
프로덕션 빌드는 `npm run build`, 빌드 미리보기는 `npm run preview`입니다.

## 현재 연출

- 별도 평면 갤러리와 01–06 선택 UI를 제거하고, 원본 Codrops 데모처럼 원통의 외부에서 내부로 진입한 뒤 다시 빠져나오는 연속 스크롤 연출로 구성했습니다.
- 스크롤 길이 500svh, 총 회전 28.27라디안(약 4.5회전), 카메라의 1 → 1 → 2 → 3.5 → 1 구간과 곡선은 Codrops Demo 1 원본을 따릅니다. 외부 → 위쪽 → 원통 접근 → 내부 → 외부로 이어집니다.
- 6개 이미지를 원통 표면에 두 번 반복합니다. 텍스처는 한 벌만 사용하므로 반복 때문에 해상도가 낮아지지 않습니다.
- 원통 높이 2, 반경 2.5, 둘레 분할 64를 원본과 동일하게 사용합니다. PNG 파일은 그대로 유지하며 원통 셀에서는 비율을 보존하는 `cover` 방식으로 표시합니다. 원통 높이에 맞추기 위해 이미지 좌우 일부는 잘릴 수 있습니다.
- 캔버스는 모든 장면에서 화면 전체를 사용합니다. 첫 진입 화면에서만 원통 아래에 소개 문구를 배치하고, 헤더·문구의 실제 높이로 원통의 투영 범위를 조정합니다. 첫 정지 구간이 끝나기 전에 원본 시야각과 중심으로 부드럽게 돌아오며 이후 카메라 경로·시야각·전체 화면 구도는 원본과 같습니다.
- 원통의 안쪽과 바깥쪽 모두 글자가 올바른 방향으로 표시됩니다.
- 배경은 제공된 DDP KN0T 갤러리의 실제 배경 이미지 3종과 안개 이미지를 사용합니다. 도입부의 붉은 열기 띠와 이어지는 붉은 배경을 스크롤에 따라 전환합니다. 강조색과 대제목은 `#ED0505`를 사용합니다.
- 선은 원본 `createParticleGeometry`와 회전 속도 기반 애니메이션을 따릅니다. 12개, 반경 3.3, 각도 0.3라디안, 상단·하단 각 6개의 배치이며 `회전 변화량 × 개별 속도 × 1.5`로 이동합니다. 회전 시 원본과 같은 임계값·보간으로 나타나고 멈추면 사라집니다. 이 선의 움직임은 스크롤에 연동되며, 배경 수증기의 상시 움직임과는 분리되어 있습니다.
- 선의 재질에 첨부된 수증기 사진의 농도 변화, 여러 가닥, 부드러운 번짐과 끝부분 페이드를 반영했습니다. 사진 자체를 화면에 얹지 않으며 사진의 향 부분은 샘플링에서 제외합니다.
- 배경 수증기는 OVEN SAUNA DDP 참고 사이트의 노이즈 흐름을 바탕으로 `#ED0505`를 사용합니다. 추가 수증기 레이어의 농도를 기존의 12%로 낮추고, 흐르는 속도는 1/5로 줄였습니다. 넓고 낮은 주파수의 노이즈와 14px 블러로 배경 위에서 옅게 퍼지도록 표시합니다. 열기 띠 위의 움직이는 밝은 부분도 농도 12%·속도 1/4로 낮췄으며, 기존 배경 이미지는 유지합니다. 모든 배경 위·원통 아래에 따로 배치해 장면이 넘어가거나 스크롤을 멈춰도 계속 움직입니다. 약 30fps와 제한된 해상도로 렌더링하며, 탭을 숨기면 멈추고 동작 줄이기 설정에서는 정지된 수증기로 표시합니다.
- 투명 캔버스와 선 재질에 동일한 premultiplied alpha 합성을 사용해, 배경 합성 시 선의 불투명도가 중복으로 낮아지는 문제를 수정했습니다.
- 추가했던 슬로건 및 소개·마무리 패널을 제거했습니다.
- 상단은 OVEN SAUNA DDP 참고 사이트와 동일하게 심볼, Highman 브랜드명, 연도 캡션을 배치합니다. 제공된 SVG 워드마크는 원본 자산으로 보존합니다. HOME, ABOUT, PROJECT는 해당 카메라 구간으로 연결됩니다.
- 상단 메뉴와 하단 프레임은 참고 사이트의 1440 × 1024 좌표계를 사용합니다. 화면 너비/1440와 높이/1024 중 작은 배율로 함께 조정하며 세로 화면은 최소 0.62를 적용합니다. 메뉴의 가로 위치, 버튼 크기, 푸터 여백과 세로 화면 배치도 참고 사이트의 값을 사용합니다.
- SPACE Modeling과 Contact US는 참고 사이트와 동일한 연결 준비 상태입니다.

## 타이포그래피와 설명

- 처음 페이지를 열었을 때만 원통과 소개 문구 사이에 여백을 둡니다. 전체 장면을 잘랐던 그리드 영역을 제거했으며, 이후 문구는 원래의 중앙·하단 위치에서 등장합니다. 첫 구간을 벗어나면 도입부의 배치 보정은 완전히 0이 됩니다.
- 네 구간의 제목과 설명은 사용자가 제공한 `HEAT AS IDENTITY`, `MELTED FORM`, `VISUAL FLOW`, `SWEAT OUT, GATHER IN` 문구를 그대로 사용합니다. 첫 문구는 처음부터 표시하고 첫 구간 끝에서 퇴장하며, 이후 설명의 등장·유지·퇴장 비율은 20/60/20입니다.
- 영문 제목은 `Highman Trial` Regular와 `#ED0505`를 사용합니다. 사각형 바탕 없이 글자 형태를 따라 짧은 그림자와 옅게 퍼지는 그림자만 적용해 배경과 구분합니다. 데스크톱 제목은 60px, 부연설명은 16px Regular이며 설명에는 그림자나 외곽선을 사용하지 않습니다.
- 본문에는 `Archivo` Regular(400)를 지정합니다. Archivo에는 한글 글리프가 없으므로 한글은 함께 포함한 `Pretendard` Regular로 표시됩니다. 상단 패널의 실제 높이를 측정해 제목이 겹치지 않게 공간을 확보합니다.
- 모바일은 제목 30px, 본문 16px를 사용합니다. 첫 설명의 실제 줄바꿈 높이에 맞춰 원통과 문구 사이의 공간을 확보합니다. 화면 높이 560px 이하에서 첫 소개 제목은 32px·본문은 16px로 표시합니다.
- 폰트는 로컬 파일로 제공하며 Archivo와 Pretendard의 라이선스 파일을 `public/brand/`에 포함했습니다.

기반 코드: [JosephASG / codrops-cinematic-scroll-animations](https://github.com/JosephASG/codrops-cinematic-scroll-animations). 원본 설명과 크레딧은 `README.md`를 참고하세요.

## Image Tube 갤러리

- 기존 시네마틱 페이지는 `/`에 그대로 보존하며 새 갤러리는 `/#/tube`에서 열립니다.
- [Matis Dené의 Codrops Image Tube](https://github.com/matdn/helmet/blob/main/src/components/FiberScene.tsx)를 참고해 반경 4, 한 줄 12개, 5줄을 세 번 반복하는 원통 배치를 사용합니다. 행마다 다른 자동 회전 속도를 적용했습니다. 마우스를 올려도 회전 속도는 바뀌지 않습니다. 세로 스크롤은 기존 시네마틱 장면으로 모이는 전환을 진행합니다.
- 여섯 개의 제공 그래픽을 각 이미지의 실제 가로세로 비율로 표시합니다. 원통의 안팎에서 글자가 뒤집히지 않도록 뒷면 UV를 보정했습니다.
- 중앙은 사용자가 제공한 SVG를 원본 형태 그대로 얕게 돌출한 OVEN SAUNA 로고입니다. 로고는 `#ED0505`를 사용하고 포인터에 따라 작은 각도로 움직입니다.
- 기존 상단·하단 패널, 로컬 폰트, 붉은 배경, 매우 옅고 느린 수증기를 공유합니다.
- 휠, 터치/마우스 드래그, 방향키·Page Up/Down으로 탐색할 수 있습니다. Pause로 자동 회전을 멈추며, 동작 줄이기 설정에서는 자동 회전이 없습니다. WebGL을 사용할 수 없으면 여섯 그래픽의 평면 목록을 제공합니다.
- 새 갤러리의 HOME, ABOUT, PROJECT는 기존 시네마틱 페이지의 해당 구간으로 연결됩니다.

## 튜브 → 시네마틱 연결

- `/#/tube`에서 아래로 스크롤하면 여러 줄의 카드가 높이를 줄이며 하나의 원통으로 모입니다. 중앙 SVG 로고는 먼저 페이드아웃하고 카드가 기존 원통의 곡면으로 휘어집니다.
- 전환 분량은 화면 높이의 2.2배(최소 1,600px)입니다. 휠·트랙패드·세로 드래그·방향키에 대응하며 완료 전에는 역방향 스크롤로 되돌릴 수 있습니다. 속도를 유지하는 임계 감쇠 보간으로 휠 입력과 역방향 입력의 가속·감속을 연결합니다. 전환 속도는 초당 약 0.38 이하로 제한하며 5차 곡선으로 각 단계의 시작과 끝을 완만하게 처리합니다. 자동 회전은 합쳐지기 시작한 뒤 서서히 잦아들고, 각 카드의 목표 회전각을 고정해 방향이 갑자기 바뀌는 현상을 방지합니다. 화면 밖 카드는 그리지 않아 전환 중 렌더링 부담을 줄였습니다.
- 기존 OGL 원통을 미리 준비해 두고, R3F 카드의 마지막 배치·64분할 곡면·회전각·반응형 반경·카메라 렌즈를 맞춥니다. 카드가 맞춰진 뒤 렌더러를 연결하므로 기존 장면을 새로 마운트하거나 다시 로딩하지 않습니다.
- 연결 완료 후 주소는 `/`로 바뀌고 기존 500svh 시네마틱 스크롤을 계속합니다. `/`를 직접 열면 도입부 없이 기존 페이지로 시작합니다.
- 폰트와 두 갤러리가 준비된 뒤 전환 입력을 받습니다. 동작 줄이기 설정에서는 공간 이동 대신 짧은 페이드로 연결합니다. WebGL 대체 목록에는 기존 이야기로 이동하는 버튼이 제공됩니다.
- 전환 과정에서도 상단·하단 프레임과 배경은 하나만 사용합니다. 기존의 수증기 농도, 설명 문구, 타이포그래피와 원통 아래 여백은 변경하지 않았습니다.
