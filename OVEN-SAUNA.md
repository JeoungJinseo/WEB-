# OVEN SAUNA 이미지 교체본

첫 번째 데모(`/`)에 제공된 그래픽 6개를 다음 순서로 적용했습니다.

1. 스티커
2. 티켓
3. 표지판
4. 눈
5. 김 서린 유리
6. 수건

## 실행

Node.js 22.12 이상에서 프로젝트 폴더를 열고 실행합니다.

```sh
npm install
npm run dev
```

브라우저에서 터미널에 표시된 로컬 주소를 열고 아래로 스크롤합니다.
프로덕션 빌드는 `npm run build`, 빌드 미리보기는 `npm run preview`입니다.

## 비율 유지 방식

- `public/oven-sauna/`에는 제공된 PNG 원본을 수정 없이 복사했습니다.
- 4:5 타일에 전체 이미지를 `contain` 방식으로 넣습니다. 원본마다 미세하게 다른 비율도 유지하며 잘라내지 않습니다.
- 원통 높이를 `둘레 × 타일 높이 ÷ (타일 너비 × 6)`으로 계산하여 텍스처와 원통 표면의 비율을 일치시켰습니다.
- 데스크톱과 모바일 모두 원통의 세 축을 같은 비율로 축소·확대합니다.
- 원통의 안쪽과 바깥쪽에서 글자가 좌우 반전되지 않도록 각 이미지의 가로 좌표를 보정했습니다.
- 도입부의 원통 회전과 카메라 이동 뒤에 `Inside the Universe` 그래픽 감상 구간으로 이어집니다. 이 구간에서는 원본 PNG가 한 장씩 평면으로 크게 표시되고 충분한 정지 구간을 가진 뒤 다음 장으로 넘어갑니다.
- 하단 01–06 버튼으로 각 그래픽에 바로 이동할 수 있습니다. HOME, ABOUT, PROJECT는 해당 스크롤 구간으로 연결됩니다.
- 상단 프레임, 로고와 글꼴은 사용자가 제공한 OVEN SAUNA 참고 사이트에 맞췄습니다. SPACE Modeling과 Contact US는 참고 사이트와 동일하게 연결 준비 상태입니다.
- 원본의 김 서린 유리 이미지를 배경 질감으로 활용하고 붉은 조명과 느리게 흐르는 빛을 추가했습니다. 그래픽 감상 구간에서는 이미지 위를 덮는 효과 없이 원본 색과 비율을 유지합니다.
- 움직임 줄이기 설정을 존중하며 WebGL을 사용할 수 없어도 그래픽 감상 구간을 사용할 수 있습니다.

이미지 목록과 크기 설정은 `src/lib/variant-1/data.ts`, 비율에 맞춘 그리기는 `utils.ts`, 이미지 방향 보정은 `shaders.ts`에 있습니다.

기반 코드: [JosephASG / codrops-cinematic-scroll-animations](https://github.com/JosephASG/codrops-cinematic-scroll-animations). 원본 설명과 크레딧은 `README.md`를 참고하세요.
