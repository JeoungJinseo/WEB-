# OVEN SAUNA 이미지 교체본

첫 번째 데모(`/`)에 제공된 그래픽 6개를 다음 순서로 적용했습니다.

1. 스티커
2. 티켓
3. 표지판
4. 눈
5. 김 서린 유리
6. 수건

## 실행

Node.js 22.12 이상에서 프로젝트 폴더를 열고 실행합니다.

```sh
npm install
npm run dev
```

브라우저에서 터미널에 표시된 로컬 주소를 열고 아래로 스크롤합니다.
프로덕션 빌드는 `npm run build`, 빌드 미리보기는 `npm run preview`입니다.

## 현재 연출

- 별도 평면 갤러리와 01–06 선택 UI를 제거하고, 원본 Codrops 데모처럼 원통의 외부에서 내부로 진입한 뒤 다시 빠져나오는 연속 스크롤 연출로 구성했습니다.
- 스크롤 길이 500svh, 총 회전 28.27라디안(약 4.5회전), 카메라의 1 → 1 → 2 → 3.5 → 1 구간과 곡선은 Codrops Demo 1 원본을 따릅니다. 외부 → 위쪽 → 원통 접근 → 내부 → 외부로 이어집니다.
- 6개 이미지를 원통 표면에 두 번 반복합니다. 텍스처는 한 벌만 사용하므로 반복 때문에 해상도가 낮아지지 않습니다.
- 원통 높이를 `둘레 × 타일 높이 ÷ (타일 너비 × 6 × 2)`로 계산합니다. PNG 원본은 그대로 유지하며 `contain` 방식으로 전체를 배치해 자르거나 늘리지 않습니다.
- 데스크톱과 모바일 모두 균일한 3D 스케일과 실제 화면 비율을 사용합니다. 기본 카메라 거리와 시야각은 원본의 반응형 설정을 따릅니다.
- 원통의 안쪽과 바깥쪽 모두 글자가 올바른 방향으로 표시됩니다.
- 배경은 제공된 DDP KN0T 갤러리의 실제 배경 이미지 3종과 안개 이미지를 사용합니다. 도입부의 붉은 열기 띠와 이어지는 붉은 배경을 스크롤에 따라 전환합니다. 로고·제목·강조색은 `#ED0505`를 유지합니다.
- 원통 위아래에 12개의 부드러운 수증기 선을 추가했습니다. 삼각형 띠의 중심과 가장자리 농도를 다르게 표현하며, 멈춰 있어도 흐르고 스크롤 속도에 따라 은은하게 농도가 바뀝니다. 화면 위에도 증기 선 3개를 더했습니다. 진한 실선이 되지 않도록 낮은 투명도, 중심부 블러, 넓게 퍼지는 주변 번짐과 끝부분 페이드를 사용합니다.
- 추가했던 슬로건 및 소개·마무리 패널을 제거했습니다.
- 제공된 SVG 워드마크를 상단 로고로 사용합니다. 형태와 비율을 유지하고 색상만 `#ED0505`로 맞췄습니다. HOME, ABOUT, PROJECT는 해당 카메라 구간으로 연결됩니다.
- SPACE Modeling과 Contact US는 참고 사이트와 동일한 연결 준비 상태입니다.

## 타이포그래피와 설명

- 제목·설명을 가운데 정렬하며, 원본의 상단 → 중앙 → 중앙 → 하단 위치와 등장·유지·퇴장 비율(20/60/20)을 사용합니다. 설명은 핵심 가치, 형태, 그래픽 시스템, 재질 표현에 관한 내용입니다.
- 영문 제목은 `Highman Trial` Regular를 유지하고 붉은색으로 표시합니다. 큰 제목이 투박하다는 피드백을 반영해 데스크톱 60px로 줄였으며 본문은 20px로, 원본과 같은 3:1 비율입니다.
- 본문에는 `Archivo` Regular(400)를 지정합니다. Archivo에는 한글 글리프가 없으므로 한글은 함께 포함한 `Pretendard` Regular로 표시됩니다. 흰색과 부드러운 배경 음영으로 설명의 가독성을 높였습니다.
- 모바일은 원본처럼 제목 30px, 본문 16px를 사용합니다. 줄바꿈은 화면 너비에 맞춥니다.
- 폰트는 로컬 파일로 제공하며 Archivo와 Pretendard의 라이선스 파일을 `public/brand/`에 포함했습니다.

기반 코드: [JosephASG / codrops-cinematic-scroll-animations](https://github.com/JosephASG/codrops-cinematic-scroll-animations). 원본 설명과 크레딧은 `README.md`를 참고하세요.
